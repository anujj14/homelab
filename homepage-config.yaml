apiVersion: v1
kind: ConfigMap
metadata:
  name: homepage-config
data:
  kubernetes.yaml: |
    mode: cluster
  settings.yaml: |
    title: "Anuj's Homelab"
    background: "https://images.unsplash.com/photo-1550684848-fac1c5b4e853?q=80&w=2560&auto=format&fit=crop"
  services.yaml: |
    # left blank for auto discovery
    []
  bookmarks.yaml: |
    []
  widgets.yaml: |
    []
